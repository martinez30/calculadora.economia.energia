server {
    listen 80;
    listen [::]:80;
    try_files $uri $uri/ /index.html;
    location ~ /.well-known/acme-challenge {
        allow all;
        root /usr/share/nginx/html;
        etag on;
    }

    root /usr/share/nginx/html;
    index index.html;
    etag on;
}
